# Porta do servidor (padrão: 3000)
PORT=3000

# Anthropic — necessário para refinamento de busca com IA
ANTHROPIC_API_KEY=sk-ant-...

# Mercado Livre — não utilizado atualmente (scraping via Playwright)
# ML_CLIENT_ID=
# ML_CLIENT_SECRET=
